<div id="projects">
	<h1>Project List</h1>
	<% Project.projects_grouped_by_location.each do |location_group| %>
		<h3><%= location_group.location%></h3>
		<ul>
		<% location_group.projects.each do |project| %>
			<li><%= link_to project.name, project_path(project.id)%></li>
		<% end %>
		</ul>
		 <br />
	<% end %>
</div>
